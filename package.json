{
  "name": "blindbond",
  "version": "0.1.0",
  "description": "Encrypted rate discovery for on-chain credit — FHE-native bond issuance on Fhenix",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:local": "npx hardhat run scripts/deploy.ts",
    "deploy:arb-sepolia": "npx hardhat run scripts/deploy.ts --network arb-sepolia"
  },
  "devDependencies": {
    "@cofhe/hardhat-plugin": "^0.4.0",
    "@nomicfoundation/hardhat-chai-matchers": "^2.0.0",
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "@typechain/ethers-v6": "^0.5.0",
    "@typechain/hardhat": "^9.0.0",
    "dotenv": "^16.4.0",
    "ethers": "^6.0.0",
    "hardhat": "^2.22.0",
    "hardhat-gas-reporter": "^1.0.8",
    "ts-node": "^10.9.0",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "@fhenixprotocol/cofhe-contracts": "^0.1.0"
  }
}
